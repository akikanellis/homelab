---
all:
  children:
    servers:
      hosts:
        server-pi:
          ansible_host: server-pi.domantaki.com
    workstations:
      hosts:
        workstation-pop-os:
          ansible_host: workstation-pop-os.domantaki.com
